# API Notes

#### Ислюченные параметры
* createComment()
  * owner_id
  * reply_to
  * guid
  
* deleteComment()
  * owner_id

* get()
  * user_id
  * offset
  * sort
  
* getById()
  * noteId
  
* getComments()
  * owner_id
  * sort
  * offset
  
* restoreComment()
  * owner_id
